name: Keep Instance Alive

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping endpoint
        run: |
          curl -f https://ck-gateway-kbjndwjdwa.cn-hangzhou.fcapp.run/health || true
